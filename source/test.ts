import { equal, deepEqual } from 'assert-helpers'
import kava from 'kava'
import Discourser from './index.js'

kava.suite('discourse-api', function (suite, test) {
	test('no-tests', function () {})
})
